const { Thing } = require('./thing.model');
const { TwoValues } = require('./two-values.model');
const { Conditional } = require('./conditional.model');
const { Nonconditional } = require('./nonconditional.model');
const { Ratio } = require('./ratio.model');

module.exports = {
    Thing,
    TwoValues,
    Conditional,
    Nonconditional,
    Ratio
}